import { BaseMapType, BaseObjectType } from "@/types/baseTypes";
import React from "react";

export default function DataVisualizer({
  objects,
  map,
}: {
  objects: BaseObjectType[];
  map: BaseMapType;
}) {
  return <div>DataVisualizer</div>;
}
